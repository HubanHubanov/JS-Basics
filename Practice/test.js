function solve(input) {


    let emptyArray = [];
    
    for (let i = 0; i < 5; i++) {
        emptyArray.push(i); // add element at the of an array; 
        // console.log(emptyArray);
    }

    let randomString = 'r-a-n-d-o-m';
    console.log(randomString.split("-").join(""))

    // split() -> transforms string to array from the elements 
    // join() -> transofmrs array to string 

    // Array.push() - add element at the end of the array 
    //Array.pop() - remove element from the end of the array
    // Array.shift() - remove element from the beginning of the array
    // Array.unshift() - add element at the beginning of the array
    


    // console.log(emptyArray);


}
solve();